unrarr
======

Recursive unrar, optionally deleting the archive files afterwards.

Test
----

Requires `shunit2`.

    git submodule update --init
    make test # optional

Installation
------------

    sudo make install
