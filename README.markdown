unrarr
======

Recursive unrar, optionally deleting the archive files afterwards.

Installation
------------

    sudo make install

Test
----

Requires `shunit2`.

    git submodule update --init
    make test # optional
